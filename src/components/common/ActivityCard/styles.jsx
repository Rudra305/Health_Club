import { Card } from "react-bootstrap";
import styled from "styled-components";

export const CardImage = styled(Card.Img)`
    height: 250px;
    width: 250px;
    transition: filter 500ms cubic-bezier(.25,.8,.25,1);
`